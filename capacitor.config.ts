import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'be.niedel.energy',
  appName: 'Local Energy',
  webDir: 'www',
  bundledWebRuntime: true,
};

export default config;
